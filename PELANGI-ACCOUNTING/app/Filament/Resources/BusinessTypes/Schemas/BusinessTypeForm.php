<?php

namespace App\Filament\Resources\BusinessTypes\Schemas;

use App\Traits\HasAutoGeneratedCode;
use Filament\Forms\Components\Select;
use Filament\Forms\Components\TextInput;
use Filament\Forms\Components\Textarea;
use Filament\Forms\Components\Toggle;
use Filament\Schemas\Components\Section;
use Filament\Schemas\Schema;

class BusinessTypeForm
{
    use HasAutoGeneratedCode;

    protected function getDocumentType(): string
    {
        return 'business_type';
    }

    protected function getCodeFieldName(): string
    {
        return 'code';
    }
    public static function configure(Schema $schema): Schema
    {
        return $schema->components([
            Section::make(__("Business Type Information"))
                ->schema([
                    TextInput::make("name")
                        ->required()
                        ->label(__("Business Type Name")),
                    (new static)->getCodeField('code', __('Business Type Code'))->required(false),
                    Textarea::make("description")
                        ->label(__("Description"))
                        ->columnSpanFull(),
                    Toggle::make("is_active")->required()->label(__("Active"))->default(true),
                    Select::make("created_by_user_id")
                        ->relationship("createdByUser", "name")
                        ->default(function () {
                            return auth()->id();
                        })
                        ->disabled()
                        ->required()
                        ->label(__("Created By")),
                ])
                ->columns(2)
                ->columnSpanFull(),
        ]);
    }
}
